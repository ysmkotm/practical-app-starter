<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.ysmkotm.practicalappstarter.mapper.DepartmentMapper">

	<resultMap id="DepartmentResultMap" type="Department">
		<id column="department_id" property="departmentId"/>
		<result column="department_code" property="departmentCode"/>
		<result column="department_name" property="departmentName"/>
		<result column="display_order" property="displayOrder"/>
		<result column="deleted_flg" property="deletedFlg"/>
		<result column="created_at" property="createdAt"/>
		<result column="updated_at" property="updatedAt"/>
	</resultMap>

	<select id="searchDepartments" resultMap="DepartmentResultMap">
		SELECT
			department_id,
			department_code,
			department_name,
			display_order,
			deleted_flg,
			created_at,
			updated_at
		FROM department
		<where>
			deleted_flg = false
			<if test="departmentCode != null">
				AND department_code LIKE CONCAT('%', REPLACE(REPLACE(REPLACE(#{departmentCode}, '\', '\\'), '%', '\%'), '_', '\_'), '%') ESCAPE '\'
			</if>
			<if test="departmentName != null">
				AND department_name LIKE CONCAT('%', REPLACE(REPLACE(REPLACE(#{departmentName}, '\', '\\'), '%', '\%'), '_', '\_'), '%') ESCAPE '\'
			</if>
		</where>
		ORDER BY display_order ASC, department_code ASC
	</select>

	<select id="findDepartments" resultMap="DepartmentResultMap">
		SELECT
			department_id,
			department_code,
			department_name,
			display_order,
			deleted_flg,
			created_at,
			updated_at
		FROM department
		WHERE deleted_flg = false
		ORDER BY display_order ASC, department_code ASC
	</select>

	<select id="findDepartmentById" resultMap="DepartmentResultMap">
		SELECT
			department_id,
			department_code,
			department_name,
			display_order,
			deleted_flg,
			created_at,
			updated_at
		FROM department
		WHERE department_id = #{departmentId}
			AND deleted_flg = false
	</select>

	<select id="findDepartmentByIdIncludingDeleted" resultMap="DepartmentResultMap">
		SELECT
			department_id,
			department_code,
			department_name,
			display_order,
			deleted_flg,
			created_at,
			updated_at
		FROM department
		WHERE department_id = #{departmentId}
	</select>

	<select id="countByDepartmentCode" resultType="int">
		SELECT COUNT(*)
		FROM department
		WHERE department_code = #{departmentCode}
			AND deleted_flg = false
		<if test="excludeDepartmentId != null">
			AND department_id != #{excludeDepartmentId}
		</if>
	</select>

	<insert id="insertDepartment" useGeneratedKeys="true" keyProperty="departmentId" keyColumn="department_id">
		INSERT INTO department (
			department_code,
			department_name,
			display_order,
			deleted_flg
		) VALUES (
			#{departmentCode},
			#{departmentName},
			#{displayOrder},
			false
		)
	</insert>

	<update id="updateDepartment">
		UPDATE department
		SET
			department_code = #{departmentCode},
			department_name = #{departmentName},
			display_order = #{displayOrder},
			updated_at = CURRENT_TIMESTAMP
		WHERE department_id = #{departmentId}
			AND deleted_flg = false
	</update>

	<update id="logicalDeleteDepartment">
		UPDATE department
		SET
			deleted_flg = true,
			updated_at = CURRENT_TIMESTAMP
		WHERE department_id = #{departmentId}
			AND deleted_flg = false
	</update>

</mapper>
